export default function HarvestDashboard() {
  return (
    <div className="absolute inset-0 pointer-events-none overflow-hidden flex flex-col justify-end">
      {/* 
        This page is now intentionally empty because all UI elements 
        (Sidebar, Header, Map Legend) are handled by the DashboardLayout.
        The map itself is also rendered in the layout.
        
        We can add specific page-level overlays here if needed in the future.
      */}
    </div>
  );
}
